fun int write_int(int x) = write(x)

fun [int] write_int_arr([int] x) = map(write_int, x)

fun [int] main() =
    let x=4/2 in
    let y=128/24 in
    let z=4/(0-2) in
    let w=(0-4)/2 in
    let v=2/4 in
    write_int_arr({x, y, z, w, v}) 
